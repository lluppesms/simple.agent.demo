# Starter Prompts

Create a new application:
```bash
I want to create an application that will showcase my dad joke collection.  This should be a web application that allows users to get a random joke and then click a button for another one, It should also let them search for specific jokes by keyword or by category. The application should have a clean and user-friendly interface, and it should be responsive so that it works well on both desktop and mobile devices.  I would like the application to be written in .NET 10 and use Blazor and Aspire technology stacks as I am familiar with that stack. The application should also include a backend API that will handle the joke retrieval and search functionality, and this API should be built using .NET 10 as well.  

This app should be deployed as an Azure Container app, so I will also need bicep and GitHub Actions code to automate the deployment process.  I would like the application to be scalable and able to handle a large number of users, so it should be designed with performance in mind.  

Finally, I would like the application to have a fun and engaging design that reflects the lighthearted nature of dad jokes, and a funny README file that will make people laugh when they visit the GitHub repository.

Use the Orchestrator agent to break down the task into smaller steps and call other agents for each step of the development process, including the frontend, backend, and deployment automation.

```
